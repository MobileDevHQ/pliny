Sequel.migration do
  change do
    create_table(:<%= table_name %>) do
      primary_key :id
      DateTime  :created_at, default: Sequel.function(:now), null: false
      DateTime  :updated_at, default: Sequel.function(:now), null: false
<% if paranoid %>
      DateTime  :deleted_at
<% end %>
    end
  end
end
